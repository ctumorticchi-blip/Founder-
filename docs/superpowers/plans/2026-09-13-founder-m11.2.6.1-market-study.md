# Plan d'implémentation — M11.2.6.1 Market Study

Spec : `docs/superpowers/specs/2026-09-13-founder-m11.2.6.1-market-study-design.md`.
Sous-jalon unique, petit périmètre — une seule tâche TDD.

## Tâche 1 — Section "Le marché" dans CompetitorsScreen.tsx

1. `web/src/data/competitorLabels.ts` : ajoute
   `competitiveIntensityLabel(value: number): string` (bucket qualitatif
   low/moderate/high sur l'intensité concurrentielle brute 0-1, réutilise
   le patron déjà présent dans ce fichier).
2. `web/src/screens/CompetitorsScreen.tsx` :
   - Titre `Concurrents identifiés` → `Étude de marché`.
   - Nouvelle section au-dessus de la liste des concurrents : appelle
     `projectMarketView(market, { finance, strategie }, rng.fork("market"))`
     (même `displayRng` déjà utilisé pour `projectCompetitorView`, un
     nouveau `fork` dédié) et affiche potentiel/croissance
     (`formatEstimateRange`, réutilisé depuis
     `strategicAccountLabels.ts`) + intensité concurrentielle
     (`competitiveIntensityLabel`).
   - Nécessite le `Market` réel de `gameState.markets[business.marketId]`
     (déjà accessible, pas de nouvelle donnée).
3. Tests (`web/test/competitorsScreen.test.tsx`, étendre le fichier
   existant plutôt qu'un nouveau) :
   - Le nouveau titre "Étude de marché" est affiché.
   - Les libellés "Potentiel mensuel estimé"/"Croissance mensuelle
     estimée"/"Intensité concurrentielle" sont présents.
   - Aucun nombre brut de `Market` (ex. `sizeMonthlyRevenuePotential`
     exact) n'apparaît tel quel — seule une fourchette formatée.
   - Les tests M11.2.5 existants (proposition, disparition d'un compte
     perdu) continuent de passer avec le nouveau titre (ajuster les
     éventuelles assertions sur l'ancien titre si nécessaire).

Vérification : suite complète web verte, typecheck/lint/build propres,
playtest mobile réel rapide (l'écran déjà playtesté en M11.2.5, vérifier
seulement que la nouvelle section s'affiche correctement et sans
débordement). Commit + push. Mise à jour `FOUNDER_STATE.md` (ajout du
sous-jalon M11.2.6.1, M11.2.6 reste ACTIF — sous-jalons 2/3 restants).
