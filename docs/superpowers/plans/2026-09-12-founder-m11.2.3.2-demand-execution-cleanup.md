# FOUNDER — M11.2.3.2 « Demand Execution Cleanup » — Plan d'implémentation

Spec : `docs/superpowers/specs/2026-09-12-founder-m11.2.3.2-demand-execution-cleanup-design.md`.
TDD strict : écrire le test, le voir échouer (raisonné ou exécuté), écrire le code minimal, vérifier vert, suivant. Commit cohérent après chaque étape validée (le commit final unique reste décidé à la toute fin, mais chaque étape doit être indépendamment vérifiable en vert).

## T0 — Test anti-ancienne-architecture (doit échouer AVANT tout changement)

**Fichier** : `test/engine/economic-models/service.test.ts`, nouveau test ajouté EN PREMIER, avant toute modification de `service.ts` :
```ts
it("ANTI-RÉGRESSION M11.2.3.2 : demande == capacité -> ventes == demande exactement (aucune seconde loterie commerciale)", () => {
  const rng = createRng(123);
  const result = ServiceEngine.computeMonth(STATE, { capacityHours: 100, targetHours: 100 }, { rng });
  expect(result.hoursSold).toBe(100);
});
```
Exécuter ce test seul et vérifier qu'il ÉCHOUE sous le code actuel (`hoursSold = 100 * utilizationRate < 100` presque toujours, sauf coïncidence de bruit) — c'est la preuve que le bug architectural existe réellement avant de le corriger. Ne pas committer ce constat isolément : il fait partie du même test file que T1.

## T1 — Service : nouvelle équation

**Fichier** : `src/engine/economic-models/service.ts`.
**Tests d'abord**, `test/engine/economic-models/service.test.ts` — réécriture complète du describe, en conservant T0 et en ajoutant :
- `demande(100) == capacité(100) -> hoursSold == 100` (T0, doit maintenant passer).
- `demande(60) < capacité(100) -> hoursSold == 60` (aucune perte).
- `demande(150) > capacité(100) -> hoursSold == 100`.
- `demande(0) -> hoursSold == 0`.
- `capacité(0) -> hoursSold == 0` (même avec demande > 0).
- `utilizationRate === hoursSold / capacityHours` exactement (métrique descriptive, testée par calcul direct, pas par borne).
- `utilizationRate === 0` quand `capacityHours === 0` (division évitée).
- déterminisme : deux `createRng` de seeds différentes (`createRng(1)` vs `createRng(999)`) avec mêmes `state`/`decisions` → `hoursSold` strictement identique (`toBe`, pas `toBeCloseTo` — plus aucun bruit).
- non-double-comptage réputation : mêmes `capacityHours`/`targetHours`/`hourlyRate`/`costPerLaborHour`, `reputationScore` différent (0.1 vs 0.9) → `hoursSold` et `revenue` strictement identiques.
- `revenue`/`variableCosts` cohérents avec `hoursSold` (test existant, conservé).
- rejet capacité/cible négative (test existant, conservé).
- **Suppression documentée** : "une meilleure réputation augmente statistiquement le taux de remplissage" — invariant devenu FAUX par construction (spec §11) ; remplacé par le test de non-double-comptage ci-dessus. Commentaire explicite dans le code du test expliquant pourquoi (jamais un retrait silencieux).
- Conserver "plusieurs mois simulés produisent un P&L plausible" (test existant, doit rester vert, valeurs différentes mais toujours plausibles).

**Implémentation** (spec §2) : supprime `BASE_UTILIZATION`, `REPUTATION_UTILIZATION_BONUS`, `UTILIZATION_NOISE_STD_DEV` ; `hoursSold = Math.min(decisions.targetHours, decisions.capacityHours)` ; `utilizationRate = decisions.capacityHours > 0 ? hoursSold / decisions.capacityHours : 0` ; renomme le paramètre `ctx` en `_ctx` (plus utilisé, `noUnusedParameters`). Met à jour le commentaire de tête du fichier (ne plus prétendre que le taux de remplissage est piloté par réputation/bruit).

## T2 — Hospitality : nouvelle équation

**Fichier** : `src/engine/economic-models/hospitality.ts`.
**Tests d'abord**, `test/engine/economic-models/hospitality.test.ts` — même structure que T1 adaptée au vocabulaire (`coversServed`/`coversCapacity`/`expectedDemandCovers`/`fillRate`) :
- ANTI-RÉGRESSION : `expectedDemandCovers(100) == coversCapacity(100) -> coversServed == 100` exactement.
- `< capacité -> coversServed == demande`.
- `> capacité -> coversServed == capacité`.
- `demande 0 -> 0`. `capacité 0 -> 0`.
- `fillRate === coversServed / coversCapacity` exactement, `0` si `coversCapacity === 0`.
- déterminisme deux seeds différentes.
- non-double-comptage réputation.
- `revenue`/`variableCosts` cohérents (test existant conservé).
- rejet valeurs négatives (test existant conservé).

**Implémentation** (spec §3) : supprime `BASE_FILL_RATE`, `REPUTATION_FILL_RATE_BONUS`, `FILL_RATE_NOISE_STD_DEV` ; `coversServed = Math.min(decisions.expectedDemandCovers, decisions.coversCapacity)` ; `fillRate = decisions.coversCapacity > 0 ? coversServed / decisions.coversCapacity : 0` ; `ctx` → `_ctx`.

## T3 — Agency : nouvelle équation

**Fichier** : `src/engine/economic-models/agency.ts`.
**Tests d'abord**, `test/engine/economic-models/agency.test.ts` :
- ANTI-RÉGRESSION : `targetMandates(10) == capacityMandates(10) -> wonMandates == 10` exactement.
- `< capacité -> wonMandates == demande`. `> capacité -> wonMandates == capacité`.
- `demande 0 -> 0`. `capacité 0 -> 0`.
- `winRate === wonMandates / capacityMandates` exactement, `0` si capacité nulle.
- déterminisme deux seeds différentes.
- non-double-comptage réputation ET `skillFactor` : mêmes `capacityMandates`/`targetMandates`/prix/coûts, `reputationScore` ET `skillFactor` différents (0.1 vs 0.9 pour les deux) → `wonMandates`/`revenue` strictement identiques.
- `variableCosts = revenue * deliveryCostRatio` (test existant conservé).
- rejet valeurs hors bornes (test existant conservé).
- **Suppression documentée** : "un skillFactor et une réputation plus élevés augmentent statistiquement le winRate" → remplacé par le test de non-double-comptage.

**Implémentation** (spec §4) : supprime `BASE_WIN_RATE`, `REPUTATION_WIN_RATE_BONUS`, `SKILL_WIN_RATE_BONUS`, `WIN_RATE_NOISE_STD_DEV` ; `wonMandates = Math.min(decisions.targetMandates, decisions.capacityMandates)` ; `winRate = decisions.capacityMandates > 0 ? wonMandates / decisions.capacityMandates : 0` ; `revenue = wonMandates * state.averageMonthlyFeePerMandate` (inchangé) ; `ctx` → `_ctx`. `AgencyEngineState.skillFactor` reste dans l'interface et dans la validation de bornes (`if (state.skillFactor < 0 || > 1) throw`), simplement plus lu par le calcul de `winRate`.

## T4 — Retail : nouvelle équation (bug distinct, cf spec §0/§5)

**Fichier** : `src/engine/economic-models/retail.ts`.
**Tests d'abord**, `test/engine/economic-models/retail.test.ts` :
- ANTI-RÉGRESSION : `expectedFootTraffic(100) == stockUnits(100) -> unitsSold == 100` exactement (sous l'ancien moteur, `conversionRate` réduisait `unitsSold` bien avant d'atteindre le stock — ce test isole précisément ce bug).
- `< capacité -> unitsSold == demande`. `> capacité -> unitsSold == capacité`.
- `demande 0 -> 0` (test existant "sans trafic, aucune vente" — conservé, toujours vrai).
- `capacité(stock) 0 -> 0`.
- `conversionRate === unitsSold / stockUnits` exactement, `0` si stock nul.
- déterminisme deux seeds différentes.
- non-double-comptage réputation.
- `revenue`/`variableCosts` cohérents (test existant conservé).
- rejet stock/trafic négatif (test existant conservé).

**Implémentation** (spec §5) : supprime `BASE_CONVERSION_RATE`, `REPUTATION_CONVERSION_BONUS`, `CONVERSION_NOISE_STD_DEV`, la variable intermédiaire `demand` ; `unitsSold = Math.min(decisions.expectedFootTraffic, decisions.stockUnits)` ; `conversionRate = decisions.stockUnits > 0 ? unitsSold / decisions.stockUnits : 0` ; `ctx` → `_ctx`. Met à jour le commentaire de tête du fichier ET le commentaire du champ `expectedFootTraffic` (ce paramètre représente désormais directement la demande déjà captée par le Demand Engine, plus un "trafic brut" à reconvertir — cohérent avec ce que `businessResolution.ts` y passe déjà depuis M11.2.3.1, aucun changement de nom de champ).

## T5 — Subscription : test de gel (aucun changement de code)

**Fichier** : `test/engine/economic-models/subscription.test.ts`, nouveau test ajouté :
```ts
it("GEL M11.2.3.2 : newSubscribers transmis sans second filtre (aucun double comptage détecté à l'inspection)", () => {
  const rng = createRng(1);
  const result = SubscriptionEngine.computeMonth(
    { ...STATE, activeSubscribers: 0, churnRate: 0 },
    { newSubscribers: 250 },
    { rng },
  );
  // Base à 0, churn à 0 : endingSubscribers doit être EXACTEMENT newSubscribers, sans réduction.
  expect(result.endingSubscribers).toBe(250);
});
```
Aucune implémentation à changer dans `subscription.ts` (spec §6). Ce test échoue seulement si une régression future réintroduit un filtre.

## T6 — Validation intermédiaire (moteurs isolés)

`npx vitest run test/engine/economic-models/` doit être intégralement vert (5 fichiers). `npm run typecheck` doit passer (vérifie que `_ctx` satisfait `noUnusedParameters` dans les 4 fichiers modifiés, et que les interfaces publiques n'ont pas bougé). Ne pas continuer vers T7 avant ce vert.

## T7 — Test d'intégration obligatoire : Service + Hospitality (énoncé §15)

**Fichier** : `test/engine/simulation/businessResolution.test.ts`, nouveau describe `"Demand Execution Cleanup — conservation exacte demande captée -> ventes (spec M11.2.3.2 §15)"`.

Construit (via `createOwnedBusiness` + `withOffer`, helpers déjà présents dans ce fichier depuis M11.2.3.1) une offre Service avec une `customerMemory` calibrée pour produire `repeatDemand` connu, et un `demandShare`/marché calibrés pour produire `newDemand` connu, tels que `newDemand + repeatDemand = 100` exactement (valeurs choisies par calcul direct à partir de `computeRepeatDemand`/`computeOfferDemand`, pas par tâtonnement empirique — si le calcul direct est trop fragile face aux nombreux facteurs de `computeOfferDemand`, utiliser un `demandShare` qui sature délibérément et lire `totalDemand` réellement produit par un premier `resolveBusinessMonth`, puis calibrer la capacité en fonction de cette valeur lue plutôt que de la deviner) :

1. `founderHoursAllocated` calibré pour que `capacityHours === totalDemand` (capacité exacte) → `sales === totalDemand`, `lostToCapacity === 0`.
2. Même offre/mémoire, capacité réduite à `0.6 × totalDemand` → `sales ≈ 0.6 × totalDemand` (capacité), `lostToCapacity ≈ 0.4 × totalDemand`, ET conservation du split : `newSales/repeatSales` dans le même ratio que `newDemand/repeatDemand` (tolérance flottante), `newSales + repeatSales ≈ sales`, `unservedNew + unservedRepeat ≈ lostToCapacity`.
3. Exemple numérique obligatoire de l'énoncé, en second test dédié : mémoire/marché calibrés pour `newDemand≈80, repeatDemand≈20` (ratio 4:1), capacité fixée à `50` → `newSales≈40 (±5%), repeatSales≈10 (±5%), unservedNew≈40 (±5%), unservedRepeat≈10 (±5%)`.

Même structure dupliquée pour Hospitality (offre `businessModel` adapté, capacité en couverts).

## T8 — Tests équivalents Retail + Agency (attention explicite du relecteur)

Même describe, même structure que T7, pour Retail (capacité en unités de stock/labor) et Agency (capacité en mandats) :
- Capacité exacte = totalDemand → `sales = totalDemand`, `lostToCapacity = 0`.
- Capacité à 60 % de totalDemand → conservation du split new/repeat.
- Le test ANTI-ANCIENNE-ARCHITECTURE doit être visible ici aussi au niveau intégration, pas seulement au niveau moteur isolé (T4) : avec l'ancien code, `sales < totalDemand` même à capacité suffisante à cause de `conversionRate`/`winRate` ; après ce jalon, `sales === totalDemand` exactement quand `capacité ≥ totalDemand`.

Retail : vérifier explicitement que la contrainte physique réelle (`min(stockUnits, laborCapacityUnits)`, déjà calculée dans `businessResolution.ts`) continue de s'appliquer normalement — un test où `stockUnits` est délibérément inférieur à `laborCapacityUnits` (et inférieur à `totalDemand`) doit donner `sales = stockUnits` (jamais plus), prouvant qu'on n'a pas supprimé par erreur une vraie contrainte en supprimant `conversionRate`.

## T9 — Réputation : non-double-comptage au niveau intégration

**Fichier** : même fichier que T7/T8, describe `"non-double-comptage de la réputation (spec M11.2.3.2 §8, §9)"`.

Deux résolutions `resolveBusinessMonth` avec exactement le même `totalDemand`/capacité/prix/coûts (même offre, même marché, même `demandShare`) mais des `familyState.reputationScore` de départ différents (`0.1` vs `0.9`) → `sales`/`revenue`/`lostToCapacity` strictement identiques (`toBe`, pas `toBeCloseTo`). Répété pour les 4 familles transactionnelles (paramétré ou 4 tests explicites).

Note : la réputation influence encore `totalDemand` lui-même (via `computeOfferDemand`) dans le jeu réel — ce test neutralise cet effet en gardant `totalDemand` identique par construction (même offre/segments/marché/demandShare), pour isoler UNIQUEMENT la conversion demande captée → vente.

## T10 — Non-régression True Repeat Demand (M11.2.3.1)

Vérifier que les tests déjà écrits en M11.2.3.1 (`test/engine/simulation/businessResolution.test.ts` describe `"allocation new/repeat — capacité proportionnelle, aucune priorité cachée"`, `test/engine/simulation/satisfactionIntegration.test.ts` describe `"True Repeat Demand — mécanismes transverses"` + les deux tests longitudinaux §16/§17) restent verts SANS modification. S'ils échouent à cause de l'augmentation mécanique des ventes (spec §10, §18), diagnostiquer précisément AVANT de toucher aux valeurs de calibration (`founderHoursAllocated`, etc.) — documenter toute recalibration nécessaire avec la même rigueur que M11.2.3.1 (jamais affaiblir un invariant pour le faire passer). Attendu à l'inspection (spec auto-revue) : `lostToCapacity` ne dépend pas de `sales`, donc ces calibrations ne devraient PAS avoir besoin de changer — à confirmer empiriquement ici, pas supposer.

## T11 — Non-régression satisfaction (M11.2.3)

Vérifier `test/engine/simulation/satisfactionIntegration.test.ts` en entier reste vert. `lastSatisfactionScore` ne dépend que de `saturationRatio = totalDemand/capacityForOffer` (inchangé par ce jalon) et du booléen `totalServed > 0` — pas de la valeur exacte de `sales`. Si un test échoue malgré ce raisonnement, documenter précisément pourquoi avant de corriger (ne jamais supposer un simple "recalibrage nécessaire" sans preuve).

## T12 — Suite complète engine

`npx vitest run` (racine) intégralement vert. `npm run typecheck`, `npm run typecheck:test`, `npm run lint`, `npm run build` verts. Comparer le nombre de tests avant/après (attendu : légère hausse nette — nouveaux tests T0-T9 moins les 2 suppressions documentées T1/T3).

## T13 — Suite complète web

`cd web && npx vitest run`, `npm run typecheck`, `npm run lint`, `npm run build` verts. Aucun changement de code web attendu pour ce jalon (aucune interface UI/storage touchée) — si un test web échoue, c'est un signal d'alerte à diagnostiquer, pas à contourner.

## T14 — Playtests Playwright iPhone 13

Deux scénarios (énoncé §17) :
1. **Hospitality** : créer un restaurant, offre lancée, calibrer une demande INFÉRIEURE à la capacité → vérifier via `localStorage`/carte UI que tous les clients captés sont servis (`lostToCapacity ≈ 0`, `sales ≈ demand`). Puis augmenter la demande (prospection ↑ ou capacité ↓) au-delà de la capacité → vérifier que seule la part réellement impossible à servir est perdue (`sales === capacity` exactement, `lostToCapacity === demand - capacity` exactement, plus de perte "aléatoire" en dessous de la capacité).
2. **Service** : scénario équivalent (offre construite, demande sous puis au-dessus de la capacité fondateur/effectif).
Vérifier que les cartes "Ce mois-ci"/"Clients" de `OfferScreen.tsx` restent cohérentes (pas de régression d'affichage, `demand`/`sales`/`lostToCapacity` toujours reliés par la même équation qu'avant, juste avec des valeurs plus élevées/exactes).

## T15 — Self-review du diff complet, puis commit + push

Relire l'intégralité de `git diff` avant de committer : vérifier qu'aucune interface publique n'a changé de forme (seulement des corps de fonction et des noms de paramètres locaux), qu'aucun `Math.random`/`ctx.rng` résiduel ne subsiste dans les 4 moteurs nettoyés, que `businessResolution.ts` n'a reçu AUCUNE modification (confirmé par `git diff --stat` : ce fichier ne doit PAS apparaître dans le diff, sauf si l'inspection T10/T11 a révélé un besoin de recalibration imprévu — auquel cas documenter précisément pourquoi). Si tout est vert : commit unique cohérent, push sur `main`.
